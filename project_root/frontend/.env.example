# Frontend environment (local development)
# Copy to frontend/.env

# Backend API URL. Use http://localhost:8080 for npm run dev.
# Leave empty for production Docker builds (same-origin requests).
VITE_API_URL=http://localhost:8080
